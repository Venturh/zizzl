overwrite: true
schema: src/types/schema.gql
documents: src/lib/**/*.gql
generates:
  src/types/graphql/index.ts:
    plugins:
      - typescript
      - typescript-operations
      - typescript-react-apollo
